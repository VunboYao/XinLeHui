const host = 'https://gy.ginmery.com/api/'

/* const config = {

  // 登录
  loginUrl: `user/${host}/login`,

  // 用code换取openId
  openIdUrl: `${host}/openid`,

  // 生成支付订单的接口
  paymentUrl: `${host}/payment`,
} */
export {
  host
}
